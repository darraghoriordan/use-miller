import React, { FunctionComponent } from "react";
type ApiErrorProps = {
    message?: string;
    children?: React.ReactNode;
};
declare const ApiError: FunctionComponent<ApiErrorProps>;
export default ApiError;
